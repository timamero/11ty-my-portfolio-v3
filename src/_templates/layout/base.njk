<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <meta name="description" content="{{ description }}">
    <link rel="canonical" href="https://www.fbcamero.com{{ page.url }}"/>
    <meta name="robots" content="index, noimageindex, nofollow" />
    <!-- Open Graph meta tags -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://www.fbcamero.com{{ page.url }}" />
    <meta property="og:title" content="{{ title }}" />
    <meta property="og:image" content="{{ img }}" />
    <meta property="og:description" content="{{ description }}" />
    
    <!-- Add Stylesheets -->
    <link rel="stylesheet" href="css/styles.css">
    
    <!-- Import fonts -->
    <title>Fatima Camero</title>
</head>
<body>
    <div class="page-content">
        {% include "sections/nav.njk" %}
        <main>{{ content | safe }}</main>
        {% include "sections/footer.njk" %}
    </div>
    

    <link href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@200;400&family=Barlow:ital,wght@0,200;0,300;0,400;1,200;1,300;1,400&family=Ubuntu+Mono:wght@400&family=Ubuntu:wght@300;400;500&display=swap" rel="stylesheet">
    <script src="js/script.js" async></script>
</body>
</html>